<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Artsy Thoughts</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://unpkg.com/easymde/dist/easymde.min.css">
<script src="https://unpkg.com/easymde/dist/easymde.min.js"></script>
  <!-- Link to the GENERATED CSS file -->
  <link rel="stylesheet" href="/css/output.css">
</head>
<body class="bg-background font-sans text-text-main">
  <header class="bg-white shadow-md">
    <nav class="container mx-auto px-6 py-4 flex justify-between items-center">
      <a href="/" class="text-2xl font-bold text-primary hover:text-accent">Artsy Thoughts</a>
      <div class="space-x-6 flex items-center">
         <a href="/articles" class="text-gray-600 hover:text-primary">All Articles</a> 
        <% if (user) { %>
            <a href="/my-articles" class="text-gray-600 hover:text-primary">My Articles</a> 
          <a href="/articles/new" class="bg-primary text-gray-600 px-4 py-2 rounded-md hover:bg-accent transition duration-300">New Article</a>
          <span class="text-gray-700">Welcome, <%= user.name %></span>
          <a href="/logout" class="text-gray-600 hover:text-primary">Logout</a>
        <% } else { %>
          <a href="/login" class="text-gray-600 hover:text-primary">Login</a>
          <a href="/register" class="bg-primary text-gray-600 px-4 py-2 rounded-md hover:bg-accent transition duration-300">Register</a>
        <% } %>
      </div>
    </nav>
  </header>
  <main class="container mx-auto px-6 py-8">
